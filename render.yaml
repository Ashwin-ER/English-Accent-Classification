services:
  - type: web
    name: english-accent-analyzer
    env: python
    buildCommand: |
      apt-get update && apt-get install -y ffmpeg
      pip install -r requirements.txt
    startCommand: streamlit run main.py --server.port=10000 --server.address=0.0.0.0
    plan: free
